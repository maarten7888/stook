# Site URLs
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database URL
# For production (Vercel), use Supabase Pooler with sslmode=require:
# DATABASE_URL=postgresql://postgres:<password>@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require&options=project%3D<ref>
# For local development, use direct connection:
DATABASE_URL=postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres?sslmode=require

# Google Cloud Vision OCR (optional - for photo import)
# JSON content of your service account key file (all on one line, escaped)
# Example: {"type":"service_account","project_id":"your-project",...}
GOOGLE_APPLICATION_CREDENTIALS_JSON=

# Optional: Google Project ID (usually extracted from credentials JSON)
# GOOGLE_PROJECT_ID=your-project-id

# Optional: OCR limits (defaults shown)
# OCR_MAX_BYTES=8000000
# OCR_ALLOWED_MIME=image/jpeg,image/png,image/webp

# Optional: Email service (Resend/Postmark)
# RESEND_API_KEY=your-resend-key
# POSTMARK_API_KEY=your-postmark-key
